# pip fallback (no pixi/conda available)
# For conda-only packages (obspy, basemap), use environment.yml or pixi.toml instead.
# NOTE: basemap cannot be installed via pip; use conda-forge.
numpy>=1.22,<2
pandas>=1.5
scipy>=1.9
matplotlib>=3.5
h5py>=3.7
jupyter>=1.0
ipykernel>=6.0
seisbench>=0.4.0
torch>=1.13.0
dask[complete]>=2022.10
tqdm>=4.64
openpyxl>=3.0
adjustText>=0.8
setuptools>=65
git+https://github.com/congcy/ELEP.git
# UW internal only — pnwstore (clone and install from source):
# git clone https://github.com/niyiyu/pnwstore.git && cd pnwstore && pip install .
