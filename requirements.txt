
# Cascadia OBS Ensemble - Core Dependencies
# For pip-only installation (without conda)
#
# Installation:
#   python -m venv venv
#   source venv/bin/activate  # On Windows: venv\Scripts\activate
#   pip install -r requirements.txt
#
# Note: For GPU support, see INSTALL.md for PyTorch installation options

# Core scientific computing
numpy>=1.22
pandas>=1.5
scipy>=1.9
matplotlib>=3.5

# Seismology
obspy>=1.4.0
seisbench>=0.4.0

# Machine learning (CPU version - see INSTALL.md for GPU)
torch>=1.13.0

# Parallel computing
dask[complete]>=2022.10.0

# Data I/O
h5py>=3.7

# Visualization
# Note: basemap is deprecated but still used in plotting scripts
# Install via: pip install basemap
# Or with conda: conda install -c conda-forge basemap
basemap>=1.3.6

# Utilities
tqdm>=4.64
openpyxl>=3.0
adjustText>=0.8

# ============================================================================
# GITHUB PACKAGES - These must be installed separately after the above
# ============================================================================
# After installing the above packages, run:
#
#   pip install git+https://github.com/congcy/ELEP.git
#   pip install git+https://github.com/niyiyu/pnwstore.git
#
# Optional (for phase association):
#   pip install git+https://github.com/seisbench/pyocto.git
#
# Or install all at once:
#   pip install git+https://github.com/congcy/ELEP.git git+https://github.com/niyiyu/pnwstore.git